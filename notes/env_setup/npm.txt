

# ------------------------------------------------------------------------------------------------------------------------------
# ------------------------------------------------------------------------------------------------------------------------------
# ---------------------------------------------------------------------------------------------------------------------- esbuild
# probably best to install esbuild globally.

npm install -g esbuild

# --- verify
esbuild --version

# 0.25.5  (as of 2025)

# ----- or put it in the project package.json :
"devDependencies": {
    "esbuild": "^0.25.5"
}

*** global eslint is less desirable, as you may use eslint plugins which would have to be local.


# ------------------------------------------------------------------------------------------------------------------------------
# ------------------------------------------------------------------------------------------------------------------------------
# -------------------------------------------------------------------------------------------------------------- npm cheat sheet


# ----- Install everything in package.json
npm i
npm install


# ----- Install everything in package.json, except devDependecies
npm install --production


# ----- install and save
npm install lodash
npm install --save-dev lodash


# ----- list packages
npm ls


# ----- package tasks
# invoke 'build' defined in package.json
npm run build





# ------------------------------------------------------------------------------------------------------------------------------
# ------------------------------------------------------------------------------------------------------------------------------
# ------------------------------------------------------------------------------------------------------------------------------





